/**
 * Public option shapes for the jaemin preset.
 *
 * Single-behavior preset (no `type` slot). Internal tuning constants
 * (rotation/scale/trigger points) are intentionally not exposed; the
 * unified `{ variant }` slot is kept for v6 schema consistency.
 */

export type JaeminVariant = "default";
