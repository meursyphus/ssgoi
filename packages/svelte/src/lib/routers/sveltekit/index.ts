/** @experimental SvelteKit route boundary. The API may change. */
export { default as SsgoiRouteBoundary } from "./ssgoi-route-boundary.svelte";
export type {
  RouteBoundaryState,
  SveltekitRouteLocation,
  SsgoiRouteBoundaryProps,
} from "./types.js";
