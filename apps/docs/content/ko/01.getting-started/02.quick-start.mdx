---
title: "빠른 시작"
description: "5분 안에 SSGOI로 첫 번째 페이지 전환 애니메이션 만들기"
nav-title: "빠른 시작"
---

## 패키지 설치

<Tabs items={[{ label: "React", value: "react" }, { label: "Svelte", value: "svelte" }, { label: "Vue", value: "vue" }, { label: "Angular", value: "angular" }]}>
  <TabPanel value="react">
    ```bash
    npm install @ssgoi/react 
    # 또는 yarn add @ssgoi/react 
    # 또는 pnpm add @ssgoi/react
    ```
  </TabPanel>
  <TabPanel value="svelte">
    ```bash
    npm install @ssgoi/svelte 
    # 또는 yarn add @ssgoi/svelte 
    # 또는 pnpm add @ssgoi/svelte
    ```
  </TabPanel>
  <TabPanel value="vue">
    ```bash
    npm install @ssgoi/vue 
    # 또는 yarn add @ssgoi/vue 
    # 또는 pnpm add @ssgoi/vue
    ```
  </TabPanel>
  <TabPanel value="angular">
    ```bash
    npm install @ssgoi/angular
    # 또는 yarn add @ssgoi/angular
    # 또는 pnpm add @ssgoi/angular
    ```
  </TabPanel>
</Tabs>

## 기본 설정 (2분)

<Tabs items={[{ label: "React", value: "react" }, { label: "Svelte", value: "svelte" }, { label: "Vue", value: "vue" }, { label: "Angular", value: "angular" }]}>
  <TabPanel value="react">
    ### 1. 루트 레이아웃 설정 (Next.js App Router)
    ```tsx
    // app/layout.tsx
    import { Ssgoi } from "@ssgoi/react";
    import { fade } from "@ssgoi/react/view-transitions";

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html>
          <body>
            <Ssgoi config={{ defaultTransition: fade() }}>
              {/* ⚠️ 중요: position: relative 필수! */}
              <div style={{ position: "relative", minHeight: "100vh" }}>
                {children}
              </div>
            </Ssgoi>
          </body>
        </html>
      );
    }
    ```
    
    > **왜 position: relative가 필요한가요?**
    > 페이지가 out 애니메이션될 때 `position: absolute`가 적용됩니다. 상위 요소에 `position: relative`가 없으면 페이지가 잘못된 위치로 이동할 수 있습니다.
    
    ### 2. 페이지 래핑
    ```tsx
    // app/page.tsx
    import { SsgoiTransition } from "@ssgoi/react";

    export default function HomePage() {
      return (
        <SsgoiTransition id="/">
          <main>
            <h1>홈 페이지</h1>
            {/* 페이지 내용 */}
          </main>
        </SsgoiTransition>
      );
    }

    // app/about/page.tsx
    export default function AboutPage() {
      return (
        <SsgoiTransition id="/about">
          <main>
            <h1>소개 페이지</h1>
            {/* 페이지 내용 */}
          </main>
        </SsgoiTransition>
      );
    }
    ```
  </TabPanel>
  
  <TabPanel value="svelte">
    ### 1. 루트 레이아웃 설정 (SvelteKit)
    ```svelte
    <!-- src/routes/+layout.svelte -->
    <script>
      import { Ssgoi } from "@ssgoi/svelte";
      import { fade } from "@ssgoi/svelte/view-transitions";
    </script>

    <Ssgoi config={{ defaultTransition: fade() }}>
      <!-- ⚠️ 중요: position: relative 필수! -->
      <div style="position: relative; min-height: 100vh;">
        <slot />
      </div>
    </Ssgoi>
    ```
    
    > **왜 position: relative가 필요한가요?**
    > 페이지가 out 애니메이션될 때 `position: absolute`가 적용됩니다. 상위 요소에 `position: relative`가 없으면 페이지가 잘못된 위치로 이동할 수 있습니다.
    
    ### 2. 페이지 래핑
    ```svelte
    <!-- src/routes/+page.svelte -->
    <script>
      import { SsgoiTransition } from "@ssgoi/svelte";
    </script>

    <SsgoiTransition id="/">
      <main>
        <h1>홈 페이지</h1>
        <!-- 페이지 내용 -->
      </main>
    </SsgoiTransition>

    <!-- src/routes/about/+page.svelte -->
    <script>
      import { SsgoiTransition } from "@ssgoi/svelte";
    </script>

    <SsgoiTransition id="/about">
      <main>
        <h1>소개 페이지</h1>
        <!-- 페이지 내용 -->
      </main>
    </SsgoiTransition>
    ```
  </TabPanel>
  <TabPanel value="vue">
    ### 1. 루트 레이아웃 설정 (Nuxt 3)

    ```vue
    <!-- app.vue -->
    <template>
      <Ssgoi :config="config">
        <!-- ⚠️ 중요: position: relative가 필수입니다! -->
        <div style="position: relative; min-height: 100vh">
          <NuxtPage />
        </div>
      </Ssgoi>
    </template>

    <script setup>
    import { Ssgoi } from "@ssgoi/vue";
    import { fade } from "@ssgoi/vue/view-transitions";

    const config = {
      defaultTransition: fade()
    };
    </script>
    ```

    > **왜 position: relative가 필요한가요?**
    > 페이지가 애니메이션 아웃할 때 `position: absolute`가 적용됩니다. 부모 요소에 `position: relative`가 없으면 페이지가 잘못된 위치로 이동할 수 있습니다.

    ### 2. 페이지 래핑

    ```vue
    <!-- pages/index.vue -->
    <template>
      <SsgoiTransition id="/">
        <main>
          <h1>홈 페이지</h1>
          <!-- 페이지 콘텐츠 -->
        </main>
      </SsgoiTransition>
    </template>

    <script setup>
    import { SsgoiTransition } from "@ssgoi/vue";
    </script>
    ```

    ```vue
    <!-- pages/about.vue -->
    <template>
      <SsgoiTransition id="/about">
        <main>
          <h1>소개 페이지</h1>
          <!-- 페이지 콘텐츠 -->
        </main>
      </SsgoiTransition>
    </template>

    <script setup>
    import { SsgoiTransition } from "@ssgoi/vue";
    </script>
    ```

  </TabPanel>
  <TabPanel value="angular">
    ### 1. 루트 컴포넌트 설정 (Angular 20+)

    ```typescript
    // app.component.ts
    import { Component } from "@angular/core";
    import { RouterOutlet } from "@angular/router";
    import { Ssgoi } from "@ssgoi/angular";
    import { fade } from "@ssgoi/angular/view-transitions";
    import type { SsgoiConfig } from "@ssgoi/angular";

    @Component({
      selector: "app-root",
      standalone: true,
      imports: [RouterOutlet, Ssgoi],
      template: `
        <!-- ⚠️ 중요: position: relative 필수! -->
        <div
          ssgoi
          [config]="ssgoiConfig"
          style="position: relative; min-height: 100vh;"
        >
          <router-outlet />
        </div>
      `,
    })
    export class AppComponent {
      ssgoiConfig: SsgoiConfig = {
        defaultTransition: fade(),
      };
    }
    ```

    > **왜 position: relative가 필요한가요?**
    > 페이지가 out 애니메이션될 때 `position: absolute`가 적용됩니다. 상위 요소에 `position: relative`가 없으면 페이지가 잘못된 위치로 이동할 수 있습니다.

    ### 2. 페이지 래핑

    ```typescript
    // home.component.ts
    import { Component } from "@angular/core";
    import { SsgoiTransition } from "@ssgoi/angular";

    @Component({
      selector: "app-home",
      standalone: true,
      imports: [SsgoiTransition],
      template: `
        <section [ssgoiTransition]="'/'">
          <main>
            <h1>홈 페이지</h1>
            <!-- 페이지 내용 -->
          </main>
        </section>
      `,
    })
    export class HomeComponent {}

    // about.component.ts
    import { Component } from "@angular/core";
    import { SsgoiTransition } from "@ssgoi/angular";

    @Component({
      selector: "app-about",
      standalone: true,
      imports: [SsgoiTransition],
      template: `
        <section [ssgoiTransition]="'/about'">
          <main>
            <h1>소개 페이지</h1>
            <!-- 페이지 내용 -->
          </main>
        </section>
      `,
    })
    export class AboutComponent {}
    ```

  </TabPanel>
</Tabs>

## 다양한 트랜지션 적용

<Tabs items={[{ label: "React", value: "react" }, { label: "Svelte", value: "svelte" }, { label: "Vue", value: "vue" }, { label: "Angular", value: "angular" }]}>
  <TabPanel value="react">
    ```tsx
    // app/layout.tsx
    import { scroll, fade, drill } from "@ssgoi/react/view-transitions";

    const ssgoiConfig = {
      transitions: [
        // 홈 → 소개: 위로 스크롤
        { from: "/", to: "/about", transition: scroll({ direction: "up" }) },
        // 소개 → 홈: 아래로 스크롤
        { from: "/about", to: "/", transition: scroll({ direction: "down" }) },
        // 목록 → 상세: 드릴 (진입)
        { from: "/list", to: "/detail/*", transition: drill({ direction: "enter" }) },
      ],
    };

    export default function RootLayout({ children }) {
      return (
        <html>
          <body>
            <Ssgoi config={ssgoiConfig}>
              <div style={{ position: "relative", minHeight: "100vh" }}>
                {children}
              </div>
            </Ssgoi>
          </body>
        </html>
      );
    }
    ```
  </TabPanel>
  <TabPanel value="svelte">
    ```svelte
    <!-- src/routes/+layout.svelte -->
    <script>
      import { Ssgoi } from "@ssgoi/svelte";
      import { scroll, fade, drill } from "@ssgoi/svelte/view-transitions";

      const ssgoiConfig = {
        transitions: [
          // 홈 → 소개: 위로 스크롤
          { from: "/", to: "/about", transition: scroll({ direction: "up" }) },
          // 소개 → 홈: 아래로 스크롤
          { from: "/about", to: "/", transition: scroll({ direction: "down" }) },
          // 목록 → 상세: 드릴 (진입)
          { from: "/list", to: "/detail/*", transition: drill({ direction: "enter" }) },
        ],
      };
    </script>

    <Ssgoi config={ssgoiConfig}>
      <div style="position: relative; min-height: 100vh;">
        <slot />
      </div>
    </Ssgoi>
    ```
  </TabPanel>
  <TabPanel value="vue">
    ```vue
    <!-- app.vue -->
    <template>
      <Ssgoi :config="ssgoiConfig">
        <div style="position: relative; min-height: 100vh">
          <NuxtPage />
        </div>
      </Ssgoi>
    </template>

    <script setup>
    import { Ssgoi } from "@ssgoi/vue";
    import { scroll, fade, drill } from "@ssgoi/vue/view-transitions";

    const ssgoiConfig = {
      transitions: [
        // 홈 → 소개: 위로 스크롤
        { from: "/", to: "/about", transition: scroll({ direction: "up" }) },
        // 소개 → 홈: 아래로 스크롤
        { from: "/about", to: "/", transition: scroll({ direction: "down" }) },
        // 리스트 → 상세: 드릴 (진입)
        { from: "/list", to: "/detail/*", transition: drill({ direction: "enter" }) },
      ],
    };
    </script>
    ```

  </TabPanel>
  <TabPanel value="angular">
    ```typescript
    // app.component.ts
    import { Component } from "@angular/core";
    import { RouterOutlet } from "@angular/router";
    import { Ssgoi } from "@ssgoi/angular";
    import { scroll, fade, drill } from "@ssgoi/angular/view-transitions";
    import type { SsgoiConfig } from "@ssgoi/angular";

    @Component({
      selector: "app-root",
      standalone: true,
      imports: [RouterOutlet, Ssgoi],
      template: `
        <div
          ssgoi
          [config]="ssgoiConfig"
          style="position: relative; min-height: 100vh;"
        >
          <router-outlet />
        </div>
      `,
    })
    export class AppComponent {
      ssgoiConfig: SsgoiConfig = {
        transitions: [
          // 홈 → 소개: 위로 스크롤
          { from: "/", to: "/about", transition: scroll({ direction: "up" }) },
          // 소개 → 홈: 아래로 스크롤
          { from: "/about", to: "/", transition: scroll({ direction: "down" }) },
          // 목록 → 상세: 드릴 (진입)
          { from: "/list", to: "/detail/*", transition: drill({ direction: "enter" }) },
        ],
      };
    }
    ```
  </TabPanel>
</Tabs>

## 완성! 🎉

축하합니다! 이제 웹사이트에 네이티브 앱 같은 페이지 전환이 적용되었습니다.
