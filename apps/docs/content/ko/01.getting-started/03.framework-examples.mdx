---
title: "프레임워크별 예제"
description: "React, Svelte 등 각 프레임워크에서 SSGOI를 사용하는 방법"
nav-title: "프레임워크 예제"
---

import { Tabs, TabPanel } from '@/components/mdx-components';

# 프레임워크별 구현 가이드

SSGOI는 여러 프레임워크를 지원합니다. 각 프레임워크별로 설치 및 사용 방법을 알아보세요.

## 설치

<Tabs items={[
  { label: "React", value: "react" },
  { label: "Svelte", value: "svelte" }
]}>
  <TabPanel value="react">
    ```bash
    npm install @ssgoi/react
    # 또는
    yarn add @ssgoi/react
    # 또는
    pnpm add @ssgoi/react
    ```
  </TabPanel>
  
  <TabPanel value="svelte">
    ```bash
    npm install @ssgoi/svelte
    # 또는
    yarn add @ssgoi/svelte
    # 또는
    pnpm add @ssgoi/svelte
    ```
  </TabPanel>
</Tabs>

## 기본 설정

<Tabs items={[
  { label: "React", value: "react" },
  { label: "Svelte", value: "svelte" }
]}>
  <TabPanel value="react">
    ### 1. App 래퍼 설정
    
    ```tsx
    import { Ssgoi } from '@ssgoi/react';
    import { fade } from '@ssgoi/react/view-transitions';

    export default function App() {
      return (
        <Ssgoi config={{ defaultTransition: fade() }}>
          <div style={{ position: 'relative' }}>
            {/* 여기에 앱 컴포넌트 */}
          </div>
        </Ssgoi>
      );
    }
    ```

    ### 2. 페이지 래퍼 추가
    
    ```tsx
    import { SsgoiTransition } from '@ssgoi/react';

    export default function HomePage() {
      return (
        <SsgoiTransition id="/home">
          <h1>홈 페이지</h1>
          {/* 페이지 내용 */}
        </SsgoiTransition>
      );
    }
    ```
  </TabPanel>
  
  <TabPanel value="svelte">
    ### 1. 레이아웃 설정
    
    ```svelte
    <!-- +layout.svelte -->
    <script>
      import { Ssgoi } from '@ssgoi/svelte';
      import { fade } from '@ssgoi/svelte/view-transitions';
    </script>

    <Ssgoi config={{ defaultTransition: fade() }}>
      <div style="position: relative; min-height: 100vh;">
        <slot />
      </div>
    </Ssgoi>
    ```

    ### 2. 페이지 설정
    
    ```svelte
    <!-- +page.svelte -->
    <script>
      import { SsgoiTransition } from '@ssgoi/svelte';
      import { page } from '$app/stores';
    </script>

    <SsgoiTransition id={$page.url.pathname}>
      <h1>홈 페이지</h1>
      <!-- 페이지 내용 -->
    </SsgoiTransition>
    ```
  </TabPanel>
</Tabs>

## SSR 프레임워크 통합

### Next.js App Router

<Tabs items={[
  { label: "layout.tsx", value: "layout" },
  { label: "page.tsx", value: "page" }
]}>
  <TabPanel value="layout">
    ```tsx
    // app/layout.tsx
    import { Ssgoi } from '@ssgoi/react';
    import { slide } from '@ssgoi/react/view-transitions';

    export default function RootLayout({ 
      children 
    }: { 
      children: React.ReactNode 
    }) {
      return (
        <html lang="ko">
          <body>
            <Ssgoi config={{
              defaultTransition: slide({ direction: 'left' })
            }}>
              <div style={{ position: 'relative', minHeight: '100vh' }}>
                {children}
              </div>
            </Ssgoi>
          </body>
        </html>
      );
    }
    ```
  </TabPanel>
  
  <TabPanel value="page">
    ```tsx
    // app/products/page.tsx
    import { SsgoiTransition } from '@ssgoi/react';

    export default function ProductsPage() {
      return (
        <SsgoiTransition id="/products">
          <h1>제품 목록</h1>
          {/* 페이지 내용 */}
        </SsgoiTransition>
      );
    }
    ```
  </TabPanel>
</Tabs>

### SvelteKit

<Tabs items={[
  { label: "+layout.svelte", value: "layout" },
  { label: "+page.svelte", value: "page" }
]}>
  <TabPanel value="layout">
    ```svelte
    <!-- src/routes/+layout.svelte -->
    <script>
      import { Ssgoi } from '@ssgoi/svelte';
      import { scale } from '@ssgoi/svelte/view-transitions';
      
      const config = {
        defaultTransition: scale(),
        transitions: [
          {
            from: '/',
            to: '/about',
            transition: slide({ direction: 'left' }),
            symmetric: true
          }
        ]
      };
    </script>

    <Ssgoi {config}>
      <div style="position: relative; min-height: 100vh;">
        <slot />
      </div>
    </Ssgoi>
    ```
  </TabPanel>
  
  <TabPanel value="page">
    ```svelte
    <!-- src/routes/products/+page.svelte -->
    <script>
      import { SsgoiTransition } from '@ssgoi/svelte';
      import { page } from '$app/stores';
    </script>

    <SsgoiTransition id={$page.url.pathname}>
      <h1>제품 목록</h1>
      <!-- 페이지 내용 -->
    </SsgoiTransition>
    ```
  </TabPanel>
</Tabs>

## 고급 사용법

### 조건부 전환 효과

<Tabs items={[
  { label: "React", value: "react" },
  { label: "Svelte", value: "svelte" }
]}>
  <TabPanel value="react">
    ```tsx
    import { Ssgoi } from '@ssgoi/react';
    import { fade, slide, scale } from '@ssgoi/react/view-transitions';

    const config = {
      transitions: [
        // 홈에서 상세 페이지로
        {
          from: '/products',
          to: '/products/*',
          transition: scale()
        },
        // 탭 간 이동
        {
          from: '/dashboard/overview',
          to: '/dashboard/analytics',
          transition: slide({ direction: 'left' })
        },
        // 양방향 설정
        {
          from: '/blog',
          to: '/blog/post/*',
          transition: pinterest(),
          symmetric: true
        }
      ],
      defaultTransition: fade()
    };

    export default function App() {
      return (
        <Ssgoi config={config}>
          {/* 앱 내용 */}
        </Ssgoi>
      );
    }
    ```
  </TabPanel>
  
  <TabPanel value="svelte">
    ```svelte
    <script>
      import { Ssgoi } from '@ssgoi/svelte';
      import { fade, slide, scale } from '@ssgoi/svelte/view-transitions';

      const config = {
        transitions: [
          // 홈에서 상세 페이지로
          {
            from: '/products',
            to: '/products/*',
            transition: scale()
          },
          // 탭 간 이동
          {
            from: '/dashboard/overview',
            to: '/dashboard/analytics',
            transition: slide({ direction: 'left' })
          },
          // 양방향 설정
          {
            from: '/blog',
            to: '/blog/post/*',
            transition: pinterest(),
            symmetric: true
          }
        ],
        defaultTransition: fade()
      };
    </script>

    <Ssgoi {config}>
      <!-- 앱 내용 -->
    </Ssgoi>
    ```
  </TabPanel>
</Tabs>

### 요소 애니메이션

<Tabs items={[
  { label: "React", value: "react" },
  { label: "Svelte", value: "svelte" }
]}>
  <TabPanel value="react">
    ```tsx
    import { transition } from '@ssgoi/react';
    import { fadeIn, slideUp } from '@ssgoi/react/transitions';

    function Card({ show }: { show: boolean }) {
      if (!show) return null;
      
      return (
        <div 
          ref={transition({
            key: 'card-1',
            in: fadeIn(),
            out: slideUp()
          })}
          className="card"
        >
          <h3>애니메이션 카드</h3>
          <p>마운트/언마운트 시 애니메이션</p>
        </div>
      );
    }
    ```
  </TabPanel>
  
  <TabPanel value="svelte">
    ```svelte
    <script>
      import { transition } from '@ssgoi/svelte';
      import { fadeIn, slideUp } from '@ssgoi/svelte/transitions';
      
      export let show = false;
    </script>

    {#if show}
      <div 
        use:transition={{
          key: 'card-1',
          in: fadeIn(),
          out: slideUp()
        }}
        class="card"
      >
        <h3>애니메이션 카드</h3>
        <p>마운트/언마운트 시 애니메이션</p>
      </div>
    {/if}
    ```
  </TabPanel>
</Tabs>

## 다음 단계

- [요소 애니메이션](/ko/docs/core-concepts/element-transitions) 자세히 알아보기
- [View Transitions](/ko/docs/core-concepts/view-transitions) 이해하기
- [전환 효과 목록](/ko/docs/transitions) 확인하기