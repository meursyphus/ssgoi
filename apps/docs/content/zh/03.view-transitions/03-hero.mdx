---
title: "Hero 过渡"
description: "通过共享元素的连续性保持上下文的过渡"
nav-title: "Hero"
---

# Hero 过渡

Hero 过渡是两个屏幕之间共享元素自然移动和变形的**连续性过渡（Continuity Transition）**。元素从一个屏幕"旅行"到另一个屏幕，为用户提供强大的视觉连接和上下文。

## 演示

<ViewTransitionDemo type="hero" />

## UX 原则

### 何时使用？

Hero 过渡用于**连续性和上下文保持**重要的场景。

#### 内容关系适用性

<SuitabilityTable
  items={[
    {
      label: "无关内容",
      suitable: "no",
      description: "没有共享元素时不适用"
    },
    {
      label: "兄弟关系",
      suitable: "maybe",
      description: "在画廊等特定情况下有效"
    },
    {
      label: "层级关系",
      suitable: "yes",
      description: "最适合列表→详情等同一对象的不同视图"
    }
  ]}
/>

#### 主要用例

- **画廊 → 全屏**：图片放大并切换到详细视图
- **产品列表 → 详情**：产品卡片扩展为详情页面
- **个人资料 → 编辑**：保持个人资料照片并切换到编辑模式
- **媒体播放器**：从迷你播放器扩展到完整播放器

### 为什么这样工作？

1. **视觉连续性**：跟踪元素移动以保持方向感
2. **表达空间关系**：从小到大，从摘要到详细
3. **最小化认知负担**：自然变形代替突然变化
4. **兴趣和愉悦**：动态动效增加用户参与度

### 动效设计

```
Hero 元素的旅程：
1. 起点 → 记录位置、大小、形状
2. 变形中 → 平滑插值动画
3. 终点 → 定位到新位置和大小

其他元素：
- 离开页面：淡出
- 进入页面：Hero 元素定位后淡入
```

## 基本用法

### 1. 过渡设置

<Tabs items={[{ label: "React", value: "react" }, { label: "Svelte", value: "svelte" }, { label: "Angular", value: "angular" }]}>
  <TabPanel value="react">
    ```tsx
    import { Ssgoi } from '@ssgoi/react';
    import { hero } from '@ssgoi/react/view-transitions';

    const config = {
      transitions: [
        {
          from: '/gallery',
          to: '/gallery/*',
          transition: hero(),
          symmetric: true
        }
      ]
    };

    export default function App() {
      return (
        <Ssgoi config={config}>
          {/* 应用内容 */}
        </Ssgoi>
      );
    }
    ```
  </TabPanel>
  <TabPanel value="svelte">
    ```svelte
    <script>
      import { Ssgoi } from '@ssgoi/svelte';
      import { hero } from '@ssgoi/svelte/view-transitions';

      const config = {
        transitions: [
          {
            from: '/gallery',
            to: '/gallery/*',
            transition: hero(),
            symmetric: true
          }
        ]
      };
    </script>

    <Ssgoi {config}>
      <slot />
    </Ssgoi>
    ```
  </TabPanel>
  <TabPanel value="angular">
    ```typescript
    import { Component } from "@angular/core";
    import { RouterOutlet } from "@angular/router";
    import { Ssgoi } from "@ssgoi/angular";
    import { hero } from "@ssgoi/angular/view-transitions";
    import type { SsgoiConfig } from "@ssgoi/angular";

    @Component({
      selector: "app-root",
      standalone: true,
      imports: [RouterOutlet, Ssgoi],
      template: `
        <div
          ssgoi
          [config]="ssgoiConfig"
          style="position: relative; min-height: 100vh;"
        >
          <router-outlet />
        </div>
      `
    })
    export class AppComponent {
      ssgoiConfig: SsgoiConfig = {
        transitions: [
          {
            from: '/gallery',
            to: '/gallery/*',
            transition: hero(),
            symmetric: true
          }
        ]
      };
    }
    ```
  </TabPanel>
</Tabs>

### 2. 元素标记

为共享元素添加 `data-hero-key` 属性：

<Tabs items={[{ label: "React", value: "react" }, { label: "Svelte", value: "svelte" }, { label: "Angular", value: "angular" }]}>
  <TabPanel value="react">
    ```tsx
    // 列表页面
    function ProductList() {
      return (
        <div className="grid">
          {products.map(product => (
            <Link key={product.id} to={`/product/${product.id}`}>
              <img 
                data-hero-key={`product-${product.id}`}
                src={product.image}
                alt={product.name}
              />
              <h3>{product.name}</h3>
            </Link>
          ))}
        </div>
      );
    }

    // 详情页面
    function ProductDetail({ product }) {
      return (
        <div>
          <img 
            data-hero-key={`product-${product.id}`}
            src={product.image}
            alt={product.name}
            className="w-full"
          />
          <h1>{product.name}</h1>
          <p>{product.description}</p>
        </div>
      );
    }
    ```
  </TabPanel>
  <TabPanel value="svelte">
    ```svelte
    <!-- 列表页面 -->
    <div class="grid">
      {#each products as product}
        <a href="/product/{product.id}">
          <img 
            data-hero-key="product-{product.id}"
            src={product.image}
            alt={product.name}
          />
          <h3>{product.name}</h3>
        </a>
      {/each}
    </div>

    <!-- 详情页面 -->
    <div>
      <img 
        data-hero-key="product-{product.id}"
        src={product.image}
        alt={product.name}
        class="w-full"
      />
      <h1>{product.name}</h1>
      <p>{product.description}</p>
    </div>
    ```
  </TabPanel>
  <TabPanel value="angular">
    ```typescript
    import { Component, Input } from "@angular/core";
    import { NgFor } from "@angular/common";
    import { Router, RouterLink } from "@angular/router";

    interface Product {
      id: string;
      name: string;
      description: string;
      image: string;
    }

    // List Page
    @Component({
      selector: "app-product-list",
      standalone: true,
      imports: [NgFor, RouterLink],
      template: `
        <div class="grid">
          <a
            *ngFor="let product of products"
            [routerLink]="['/product', product.id]"
          >
            <img
              [attr.data-hero-key]="'product-' + product.id"
              [src]="product.image"
              [alt]="product.name"
            />
            <h3>{{ product.name }}</h3>
          </a>
        </div>
      `
    })
    export class ProductListComponent {
      products: Product[] = PRODUCTS; // Provide product data from your store/service
    }

    // Detail Page
    @Component({
      selector: "app-product-detail",
      standalone: true,
      template: `
        <div>
          <img
            [attr.data-hero-key]="'product-' + product.id"
            [src]="product.image"
            [alt]="product.name"
            class="w-full"
          />
          <h1>{{ product.name }}</h1>
          <p>{{ product.description }}</p>
        </div>
      `
    })
    export class ProductDetailComponent {
      @Input() product!: Product; // Define Product model for your app
    }
    ```
  </TabPanel>
</Tabs>

## 实践示例

### 1. 图片画廊

从缩略图扩展到全屏：

<Tabs items={[{ label: "React", value: "react" }, { label: "Svelte", value: "svelte" }, { label: "Angular", value: "angular" }]}>
  <TabPanel value="react">
    ```tsx
    // 画廊网格
    function Gallery() {
      return (
        <div className="grid grid-cols-3 gap-4">
          {images.map(img => (
            <Link to={`/photo/${img.id}`} key={img.id}>
              <div 
                data-hero-key={`photo-${img.id}`}
                className="aspect-square overflow-hidden"
              >
                <img src={img.thumb} className="object-cover w-full h-full" />
              </div>
            </Link>
          ))}
        </div>
      );
    }

    // 全屏视图
    function PhotoView({ id }) {
      return (
        <div 
          data-hero-key={`photo-${id}`}
          className="fixed inset-0 bg-black"
        >
          <img src={image.full} className="w-full h-full object-contain" />
        </div>
      );
    }
    ```
  </TabPanel>
  <TabPanel value="svelte">
    ```svelte
    <!-- 画廊网格 -->
    <div class="grid grid-cols-3 gap-4">
      {#each images as img}
        <a href="/photo/{img.id}">
          <div 
            data-hero-key="photo-{img.id}"
            class="aspect-square overflow-hidden"
          >
            <img src={img.thumb} class="object-cover w-full h-full" />
          </div>
        </a>
      {/each}
    </div>

    <!-- 全屏视图 -->
    <div 
      data-hero-key="photo-{id}"
      class="fixed inset-0 bg-black"
    >
      <img src={image.full} class="w-full h-full object-contain" />
    </div>
    ```
  </TabPanel>
  <TabPanel value="angular">
    ```typescript
    import { Component, Input } from "@angular/core";
    import { NgFor } from "@angular/common";
    import { RouterLink } from "@angular/router";

    interface GalleryImage {
      id: string;
      thumb: string;
      full: string;
      title: string;
      author: string;
      description: string;
    }

    interface Photo {
      id: string;
      full: string;
      title: string;
      description: string;
    }

    // Gallery Grid
    @Component({
      selector: "app-gallery",
      standalone: true,
      imports: [NgFor, RouterLink],
      template: `
        <div class="grid grid-cols-3 gap-4">
          <a
            *ngFor="let image of images"
            [routerLink]="['/photo', image.id]"
          >
            <div
              [attr.data-hero-key]="'photo-' + image.id"
              class="aspect-square overflow-hidden"
            >
              <img [src]="image.thumb" class="object-cover w-full h-full" />
            </div>
          </a>
        </div>
      `
    })
    export class GalleryComponent {
      images: GalleryImage[] = IMAGES; // Replace with your image data source
    }

    // Fullscreen View
    @Component({
      selector: "app-photo-view",
      standalone: true,
      template: `
        <div
          [attr.data-hero-key]="'photo-' + photo.id"
          class="fixed inset-0 bg-black"
        >
          <img [src]="photo.full" class="w-full h-full object-contain" />
        </div>
      `
    })
    export class PhotoViewComponent {
      @Input() photo!: Photo; // Match your photo model
    }
    ```
  </TabPanel>
</Tabs>

### 2. 卡片扩展

从小卡片到全屏详情：

<Tabs items={[{ label: "React", value: "react" }, { label: "Svelte", value: "svelte" }, { label: "Angular", value: "angular" }]}>
  <TabPanel value="react">
    ```tsx
    // 卡片列表
    function CardGrid() {
      return (
        <div className="grid gap-6">
          {items.map(item => (
            <article 
              key={item.id}
              data-hero-key={`card-${item.id}`}
              onClick={() => navigate(`/item/${item.id}`)}
              className="bg-white rounded-lg shadow-lg p-4 cursor-pointer"
            >
              <h3>{item.title}</h3>
              <p>{item.summary}</p>
            </article>
          ))}
        </div>
      );
    }

    // 扩展详情
    function ItemDetail({ item }) {
      return (
        <article 
          data-hero-key={`card-${item.id}`}
          className="min-h-screen bg-white p-8"
        >
          <h1 className="text-4xl">{item.title}</h1>
          <p className="text-lg">{item.fullContent}</p>
        </article>
      );
    }
    ```
  </TabPanel>
  <TabPanel value="svelte">
    ```svelte
    <!-- 卡片列表 -->
    <div class="grid gap-6">
      {#each items as item}
        <article 
          data-hero-key="card-{item.id}"
          on:click={() => goto(`/item/${item.id}`)}
          class="bg-white rounded-lg shadow-lg p-4 cursor-pointer"
        >
          <h3>{item.title}</h3>
          <p>{item.summary}</p>
        </article>
      {/each}
    </div>

    <!-- 扩展详情 -->
    <article 
      data-hero-key="card-{item.id}"
      class="min-h-screen bg-white p-8"
    >
      <h1 class="text-4xl">{item.title}</h1>
      <p class="text-lg">{item.fullContent}</p>
    </article>
    ```
  </TabPanel>
  <TabPanel value="angular">
    ```typescript
    import { Component, Input } from "@angular/core";
    import { NgFor } from "@angular/common";
    import { Router } from "@angular/router";

    interface Item {
      id: string;
      title: string;
      summary: string;
      fullContent: string;
    }

    // Card List
    @Component({
      selector: "app-card-grid",
      standalone: true,
      imports: [NgFor],
      template: `
        <div class="grid gap-6">
          <article
            *ngFor="let item of items"
            (click)="open(item.id)"
            [attr.data-hero-key]="'card-' + item.id"
            class="bg-white rounded-lg shadow-lg p-4 cursor-pointer"
          >
            <h3>{{ item.title }}</h3>
            <p>{{ item.summary }}</p>
          </article>
        </div>
      `
    })
    export class CardGridComponent {
      items: Item[] = ITEMS; // Provide list data

      constructor(private readonly router: Router) {}

      open(id: string) {
        this.router.navigate(['/item', id]);
      }
    }

    // Expanded Detail
    @Component({
      selector: "app-item-detail",
      standalone: true,
      template: `
        <article
          [attr.data-hero-key]="'card-' + item.id"
          class="min-h-screen bg-white p-8"
        >
          <h1 class="text-4xl">{{ item.title }}</h1>
          <p class="text-lg">{{ item.fullContent }}</p>
        </article>
      `
    })
    export class ItemDetailComponent {
      @Input() item!: Item; // Match your item model
    }
    ```
  </TabPanel>
</Tabs>

### 3. 多个 Hero 元素

多个元素同时过渡：

<Tabs items={[{ label: "React", value: "react" }, { label: "Svelte", value: "svelte" }, { label: "Angular", value: "angular" }]}>
  <TabPanel value="react">
    ```tsx
    // 个人资料卡片
    function ProfileCard({ user }) {
      return (
        <div onClick={() => navigate(`/profile/${user.id}`)}>
          <img 
            data-hero-key={`avatar-${user.id}`}
            src={user.avatar}
            className="w-16 h-16 rounded-full"
          />
          <h3 data-hero-key={`name-${user.id}`}>
            {user.name}
          </h3>
        </div>
      );
    }

    // 个人资料详情
    function ProfileDetail({ user }) {
      return (
        <div>
          <img 
            data-hero-key={`avatar-${user.id}`}
            src={user.avatar}
            className="w-32 h-32 rounded-full"
          />
          <h1 data-hero-key={`name-${user.id}`}>
            {user.name}
          </h1>
        </div>
      );
    }
    ```
  </TabPanel>
  <TabPanel value="svelte">
    ```svelte
    <!-- 个人资料卡片 -->
    <div on:click={() => goto(`/profile/${user.id}`)}>
      <img 
        data-hero-key="avatar-{user.id}"
        src={user.avatar}
        class="w-16 h-16 rounded-full"
      />
      <h3 data-hero-key="name-{user.id}">
        {user.name}
      </h3>
    </div>

    <!-- 个人资料详情 -->
    <div>
      <img 
        data-hero-key="avatar-{user.id}"
        src={user.avatar}
        class="w-32 h-32 rounded-full"
      />
      <h1 data-hero-key="name-{user.id}">
        {user.name}
      </h1>
    </div>
    ```
  </TabPanel>
  <TabPanel value="angular">
    ```typescript
    import { Component, Input } from "@angular/core";
    import { Router } from "@angular/router";

    interface User {
      id: string;
      name: string;
      avatar: string;
    }

    // Profile Card
    @Component({
      selector: "app-profile-card",
      standalone: true,
      template: `
        <div (click)="openProfile()">
          <img
            [attr.data-hero-key]="'avatar-' + user.id"
            [src]="user.avatar"
            class="w-16 h-16 rounded-full"
          />
          <h3 [attr.data-hero-key]="'name-' + user.id">
            {{ user.name }}
          </h3>
        </div>
      `
    })
    export class ProfileCardComponent {
      @Input() user!: User; // Provide user model

      constructor(private readonly router: Router) {}

      openProfile() {
        this.router.navigate(['/profile', this.user.id]);
      }
    }

    // Profile Detail
    @Component({
      selector: "app-profile-detail",
      standalone: true,
      template: `
        <div>
          <img
            [attr.data-hero-key]="'avatar-' + user.id"
            [src]="user.avatar"
            class="w-32 h-32 rounded-full"
          />
          <h1 [attr.data-hero-key]="'name-' + user.id">
            {{ user.name }}
          </h1>
        </div>
      `
    })
    export class ProfileDetailComponent {
      @Input() user!: User;
    }
    ```
  </TabPanel>
</Tabs>

## 注意事项

### Hero 键规则

- 每个页面中 `data-hero-key` 必须**唯一**
- 键是字符串，可以动态生成
- 只有具有相同键的元素才会连接

### 性能考虑

- Hero 元素在 DOM 中被克隆并动画化
- 过多的 Hero 元素会影响性能
- 预加载图片以防止闪烁

## 无障碍性

- 减少动画设置时立即过渡
- 焦点管理支持键盘导航
- 屏幕阅读器将其识别为普通页面过渡

## 最佳实践

### ✅ 应该

- 用于视觉上重要的元素
- 应用于同一对象的不同表示
- 保持自然的大小/位置变化
- 用于引导用户视线

### ❌ 不应该

- 不要同时应用于太多元素
- 不要连接无关元素
- 避免极端的大小变化（超过 10 倍）
- 对文本内容谨慎使用