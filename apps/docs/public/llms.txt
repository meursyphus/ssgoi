# SSGOI - Page Transition Library

> Universal page transition library that works in all browsers (Chrome, Firefox, Safari), unlike the View Transition API (Chrome-only).

This document is for AI assistants to help developers set up SSGOI page transitions.

**This file is the React setup guide plus the shared core reference** (`SsgoiConfig`, transition catalog, troubleshooting — those apply to every framework). Using Svelte, Vue/Nuxt, Solid, Angular, or Qwik? Read the matching file from the Index at the bottom instead of the React sections here.

**Current core package version:** `@ssgoi/core@6.6.12`

---

## Installation (React)

```bash
npm install @ssgoi/react
```

Reference templates:

- Next.js: https://github.com/meursyphus/ssgoi/tree/main/templates/nextjs
- React Router: https://github.com/meursyphus/ssgoi/tree/main/templates/react-router
- TanStack Router: https://github.com/meursyphus/ssgoi/tree/main/templates/tanstack-router

---

## Layout Setup

SSGOI works on any web app. There are only three things to get right:

1. **Decide your scroll element** — the element whose scroll position SSGOI should preserve across transitions. Often a `<main>` or a centered content container.
2. **Apply `relative z-0` (and optionally `overflow-x-clip`) to the outer element that wraps `<Ssgoi>`.** This is the layout shell, not the `data-ssgoi-transition` boundary.
3. **Mark routed content with `data-ssgoi-transition="..."`**. React apps can use one layout-level boundary utility for this marker (next section).

Why those classes:

| Class             | Why                                                                                                      |
| ----------------- | -------------------------------------------------------------------------------------------------------- |
| `relative`        | OUT page is cloned with `position: absolute` — needs a positioned ancestor                               |
| `z-0`             | Creates a stacking context so the OUT page doesn't fall behind backgrounds                               |
| `overflow-x-clip` | Prevents horizontal overflow flashing during slide/drill (only needed if you use horizontal transitions) |

The scroll element and the `<Ssgoi>` wrapper are typically **the same element**, so all four classes (`overflow-y-auto relative z-0 overflow-x-clip`) sit together.

### Layout example (mobile-style web app)

The library is most often used to ship app-like, mobile-width experiences, so this example shows that shape — a centered max-width column where the column itself scrolls. Adapt as needed for desktop layouts.

```tsx
// src/app/ssgoi-provider.tsx
"use client";

import { type ReactNode } from "react";
import { Ssgoi } from "@ssgoi/react";
import { drill, fade } from "@ssgoi/react/view-transitions";
import { SsgoiTransitionBoundary } from "./ssgoi-transition-boundary";

const config = {
  transitions: [
    {
      priority: -100,
      on: "/**",
      except: ["/", "/about"],
      transition: drill({ type: "parallax" }),
    },
    { from: "/", to: "/about", transition: fade() },
  ],
};

export function SsgoiProvider({ children }: { children: ReactNode }) {
  return (
    <Ssgoi config={config}>
      {/* Routed content marker. Layout positioning belongs to the outer wrapper. */}
      <SsgoiTransitionBoundary className="min-h-full bg-black">
        {children}
      </SsgoiTransitionBoundary>
    </Ssgoi>
  );
}

// src/app/layout.tsx
import { type ReactNode } from "react";
import { SsgoiProvider } from "./ssgoi-provider";

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html>
      <body>
        <div className="mx-auto max-w-md h-dvh flex flex-col">
          {/* Layout shell: scroll element + positioned ancestor for OUT clones. */}
          <main className="flex-1 overflow-y-auto relative z-0 overflow-x-clip">
            <SsgoiProvider>{children}</SsgoiProvider>
          </main>
        </div>
      </body>
    </html>
  );
}
```

Building an app shell with a persistent bottom tab bar? Don't bolt the nav outside `<main>` — use the nested-boundary pattern: https://ssgoi.dev/llms/bottom-nav.txt

---

## React Route Boundary Setup

For React, Next.js, React Router, and TanStack Router, mark routed content once with a small router-specific boundary utility. The utility uses the current pathname as the logical transition id and keys the routed subtree so React gives SSGOI a clear outgoing boundary and incoming boundary. Do not put `relative z-0 overflow-x-clip` on this utility; those belong to the outer wrapper shown above.

Next.js implementation:

```tsx
"use client";

import { type ElementType, type ReactNode } from "react";
import { usePathname } from "next/navigation";

export function SsgoiTransitionBoundary({
  children,
  as,
  className,
}: {
  children: ReactNode;
  as?: ElementType;
  className?: string;
}) {
  const pathname = usePathname();
  const Component = as ?? "div";

  return (
    <Component
      key={pathname}
      data-ssgoi-transition={pathname}
      className={className}
    >
      {children}
    </Component>
  );
}
```

React Router uses the same component body, but reads `const { pathname } = useLocation()` from `react-router`. TanStack Router reads `const pathname = useRouterState({ select: (state) => state.location.pathname })`.

```tsx
import { SsgoiTransitionBoundary } from "./ssgoi-transition-boundary";

export function AppShell({ children }: { children: React.ReactNode }) {
  return (
    <SsgoiTransitionBoundary className="min-h-full bg-black">
      {children}
    </SsgoiTransitionBoundary>
  );
}
```

`SsgoiTransitionBoundary` reads the current pathname internally and sets both `key` and `data-ssgoi-transition`. SSGOI itself only needs a logical page id that matches config; using the real pathname is the React utility's default convention. Use `:id` for one dynamic segment, `*` for exactly one arbitrary segment, and a suffix `**` for zero or more segments. Thus `/post/*` matches `/post/1` but not `/post/1/comments`, while `/docs/**` matches `/docs`, `/docs/install`, and deeper paths.

---

## SsgoiConfig

```ts
type SsgoiConfig = {
  // Flat route rules. Exactly one selector shape per rule:
  // { on, except?, transition }, { from, to, transition }, or
  // { ordered, transition }. Every rule may set priority.
  transitions?: SsgoiTransitionRule[];

  // Rewrite (from, to) before matching. Useful for stripping prefixes/locales.
  middleware?: (from: string, to: string) => { from: string; to: string };

  // Scroll preservation. Defaults are sensible — see section below.
  preserveScroll?: PreserveScrollOption;
};
```

### Route rules and effect factories

Each transition factory returns one effect-only `TransitionConfig`. Route
matching and direction live in the surrounding rule:

```ts
const config = {
  transitions: [
    {
      on: "/post/**",
      except: ["/", "/about", "/contact"],
      transition: drill(),
    },
    {
      from: ["/", "/about", "/contact"],
      to: ["/", "/about", "/contact"],
      transition: fade(),
    },
    { ordered: ["/tabs/a", "/tabs/b"], transition: slide() },
    {
      from: "/grid",
      to: "/photo/:id",
      transition: zoom({ type: "expand" }),
    },
  ],
};
```

Three exclusive selector shapes:

- **`{ on, except?, transition }`** — route-family membership. Outside → inside is forward; inside → outside is backward; navigation inside the family uses popstate and semantic history. Use `except` to keep top-level routes outside a catch-all stack.
- **`{ from, to, transition, bidirectional? }`** — precise pair/families. Normal orientation is forward and reverse is backward. `bidirectional` defaults to `true`; arrays mean OR on each side.
- **`{ ordered, transition }`** — both endpoints must match the list. Increasing index is forward and decreasing index is backward.

Candidates are selected by higher explicit `priority` (default `0`), then path
specificity (exact/static > `:id` > `*` > `**`), then earlier declaration
order. Factories accept only their effect options (`type`, `variant`,
`options`); the dispatcher passes semantic direction to both `prepare` and
`animation` through `context.direction`.

### preserveScroll

**Defaults work out of the box.** Mobile viewports automatically preserve scroll position across transitions; desktop does not. You normally don't need to configure this.

**Customizing — exclude specific paths:**

```ts
const config = {
  preserveScroll: { exclude: ["/post/*"] },
};
```

When navigating _to_ a path matching `exclude`, the destination starts at scroll position 0. All other navigations keep their preserved scroll.

> **Note:** `skipAnimationOnBack` was removed in 6.0. Native swipe-back gestures are now auto-detected and animations skipped automatically on iOS.

---

## Transitions

Catalog of built-in transitions. Open the linked page for the full signature, options, and (when required) the `data-*` keys to put on your DOM.

### `drill` — https://ssgoi.dev/llms/drill.txt

iOS-style hierarchical navigation. New page slides in from the right; old page eases left underneath. **Two types**: `parallax` (depth layering, default) and `slide` (flat cross-faded slide). Use for **List → Detail** flows. (Legacy `type: "crossfade"` deprecated → use `"slide"`.)

### `fade` — https://ssgoi.dev/llms/fade.txt

Calm cross-fade between pages. No motion, just opacity. Use as a **safe default** for unrelated pages.

### `slide` — https://ssgoi.dev/llms/slide.txt

Horizontal push. Forward navigation slides left, backward slides right. Use for **tab navigation** or sequential page flows.

### `scroll` — https://ssgoi.dev/llms/scroll.txt

Vertical "page scroll" — pages slide along the y-axis. **Two types**: `directional` (forward up / backward down, default) and `non-directional` (always slides up regardless of direction). Use for **onboarding** or paginated long-form pages.

### `axis` — https://ssgoi.dev/llms/axis.txt

Material/Flutter-style shared-axis page swap. **Three types**: `x` (horizontal, default), `y` (vertical), `z` (depth). On `x`, `variant: "default"` is a relaxed fluid feel and `variant: "snappy"` is a tight tab-swap (KakaoTalk-style). On `y`, `variant: "default"` is directional (forward = bottom→top) and `variant: "non-directional"` always rises from below — use for bottom-nav tab swaps that aren't a forward/backward pair. Use for **sibling/tab routes**.

### `sheet` — https://ssgoi.dev/llms/sheet.txt

Bottom sheet style. New page slides up from below. **Three types**: `static` (background untouched, default), `scale` (background scales down behind), and `blur` (background blurs + recedes behind, a modal pushing the page out of focus). Use for **modal-like** flows (FAB → compose, "new item" screens). (Legacy `type: "background-scale"` deprecated → use `"scale"`.)

### `hero` — https://ssgoi.dev/llms/hero.txt

Shared element transition. The same logical object (thumbnail, avatar) is tweened in size and position from one page to the next. **Two types**: `static` (default — incoming chrome snaps in while a temporary shared-element clone morphs above it) and `fade` (whole pages cross-fade with the same shared-element clone floating above — use when both pages have their own chrome). **Requires matching `data-hero-enter-key` (detail side) and `data-hero-exit-key` (list side).** A directly keyed image—or one direct image child in a clipping wrapper—automatically supplies intrinsic ratio, computed centered `cover` / `contain` fit, visible crop, and simple CSS radius. Ambiguous geometry falls back to the element bounding box. Legacy single-attribute `data-hero-key` is still honored as a fallback.

### `zoom` — https://ssgoi.dev/llms/zoom.txt

Card-to-detail expansion. The tapped card grows to fill the detail viewport. **Three types**: `static` (morphs to target size only, default), `expand` (fills viewport), `blur` (background blurs while card focuses). Optional `variant: "fade"` cross-fades surrounding content. **Requires matching `data-zoom-exit-key` (list/card side) and `data-zoom-enter-key` (detail/expanded side).** A directly keyed image—or one direct image child in a clipping wrapper—automatically supplies intrinsic-ratio, centered `cover` / `contain` geometry, crop, and simple CSS radius; ambiguous geometry uses the original bounding-box animation.

### `strip` — https://ssgoi.dev/llms/strip.txt

3D Y-axis perspective flip. Pages rotate horizontally with depth perspective. Use for **carded, distinctive page swaps** where slide feels too flat.

### `blind` — https://ssgoi.dev/llms/blind.txt

Vertical/horizontal blinds wipe across, revealing the new page in staggered slats. **Two types**: `horizontal` (default) and `vertical`. Use for **cinematic, dramatic** reveals.

### `film` — https://ssgoi.dev/llms/film.txt

Film-strip style: outgoing page scales down and slides off while incoming scales up from a tile with animated corner borders. Use for **gallery / lightbox** style flows that should feel premium.

### `rotate` — https://ssgoi.dev/llms/rotate.txt

Card flip. Outgoing rotates 180° and fades; incoming counter-rotates in from -180°. Use for **playful** transitions between sibling pages.

### `jaemin` — https://ssgoi.dev/llms/jaemin.txt

Quirky: incoming page rotates in at 45° from tiny scale, then unwinds and grows. Use sparingly for **special / playful** moments — modal openings, easter-egg transitions.

---

## Patterns

### Persistent bottom nav (nested boundaries) — https://ssgoi.dev/llms/bottom-nav.txt

App shell where main tab screens share a bottom tab bar and detail screens are fullscreen without it. The nav must stand still on tab↔tab (`axis`) but slide away with the page on tab→detail (`drill`) — one provider can't do both, so you nest a second `<Ssgoi>` with a stable-key outer boundary. Full structure, code, and gotchas in the linked file.

---

## Troubleshooting

### Page jumps during transition

- **Cause**: Missing `position: relative` on container
- **Fix**: Add `relative` to the element wrapping `<Ssgoi>`

### OUT page disappears behind background

- **Cause**: Missing `z-index: 0` on container
- **Fix**: Add `z-0` to the div wrapping `{children}`

### Horizontal overflow during slide/drill transitions

- **Cause**: Missing overflow clip
- **Fix**: Add `overflow-x-clip` to container

### Transition not applying

- **Cause**: `data-ssgoi-transition` doesn't match route pattern
- **Fix**: Ensure `data-ssgoi-transition` matches an `on`, `from`, `to`, or `ordered` route pattern

### hero / zoom transition not running

- **Cause**: Data attribute keys don't match between pages, or attribute missing on one side
- **Fix**: For `hero`, list side uses `data-hero-exit-key` and detail side uses `data-hero-enter-key` (same value). For `zoom`, outgoing uses `data-zoom-exit-key` and incoming uses `data-zoom-enter-key` (same value). Legacy single-attribute `data-hero-key` on both pages is also honored.

Deeper debugging (clone & absolute mechanism, step-by-step transition flow): https://ssgoi.dev/llms/how-it-works.txt

---

## Index

Everything outside the React core above lives in its own plain-text file. Fetch only what you need.

### Per-transition reference

- drill: https://ssgoi.dev/llms/drill.txt
- fade: https://ssgoi.dev/llms/fade.txt
- slide: https://ssgoi.dev/llms/slide.txt
- scroll: https://ssgoi.dev/llms/scroll.txt
- axis: https://ssgoi.dev/llms/axis.txt
- sheet: https://ssgoi.dev/llms/sheet.txt
- hero: https://ssgoi.dev/llms/hero.txt
- zoom: https://ssgoi.dev/llms/zoom.txt
- strip: https://ssgoi.dev/llms/strip.txt
- blind: https://ssgoi.dev/llms/blind.txt
- film: https://ssgoi.dev/llms/film.txt
- rotate: https://ssgoi.dev/llms/rotate.txt
- jaemin: https://ssgoi.dev/llms/jaemin.txt

### Patterns

- Persistent bottom nav / nested boundaries: https://ssgoi.dev/llms/bottom-nav.txt

### Other frameworks (setup + route boundary rules)

- Svelte / SvelteKit: https://ssgoi.dev/llms/svelte.txt
- Vue / Nuxt: https://ssgoi.dev/llms/vue.txt
- Solid / SolidStart: https://ssgoi.dev/llms/solid.txt
- Angular: https://ssgoi.dev/llms/angular.txt
- Qwik / Qwik City: https://ssgoi.dev/llms/qwik.txt

The `SsgoiConfig` shape, transition catalog, and troubleshooting sections above are shared across all frameworks. Installation, provider wiring, and route-boundary lifecycle are documented only in the linked framework guides.

### Deep dive

- How it works (clone & absolute, transition flow): https://ssgoi.dev/llms/how-it-works.txt

### Project links

- Docs: https://ssgoi.dev/docs
- Live examples (real apps rebuilt with SSGOI): https://ssgoi.dev/showcase
- GitHub: https://github.com/meursyphus/ssgoi
- NPM: https://www.npmjs.com/package/@ssgoi/react
