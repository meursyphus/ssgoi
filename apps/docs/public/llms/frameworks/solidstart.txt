# SSGOI with SolidStart

SolidStart wiring. Choose effects from:
https://ssgoi.dev/llms/transitions.txt

```bash
npm install @ssgoi/solid
```

Shell class strings below are Tailwind shorthand. Without Tailwind, apply
`position: relative; z-index: 0; min-height: 100dvh; overflow-x: clip`. Use
`clip`, not `hidden`: `overflow-x: hidden` makes the wrapper the scroll
container, and scroll restore then targets the wrong element. Fall back to
`100vh` only where `dvh` is unsupported.

## Setup

Everything wires at the router root — one `<Ssgoi>`, the shell classes, and
the boundary component around the routed children:

```tsx
// src/app.tsx
import { Router } from "@solidjs/router";
import { FileRoutes } from "@solidjs/start/router";
import { Ssgoi } from "@ssgoi/solid";
import { config } from "./ssgoi-config";
import { SsgoiRouteBoundary } from "@ssgoi/solid/solidstart";

export default function App() {
  return (
    <Router
      root={(props) => (
        <main class="relative z-0 min-h-dvh overflow-x-clip">
          <Ssgoi config={config}>
            <SsgoiRouteBoundary>{props.children}</SsgoiRouteBoundary>
          </Ssgoi>
        </main>
      )}
    >
      <FileRoutes />
    </Router>
  );
}
```

```ts
// src/ssgoi-config.ts
import type { SsgoiConfig } from "@ssgoi/solid";
import { drill } from "@ssgoi/solid/view-transitions";

export const config: SsgoiConfig = {
  transitions: [{ on: "/**", except: "/", transition: drill() }],
};
```

## The boundary component

`<Show keyed>` recreates its child when the id changes — that unmount/mount
pair is what SSGOI animates:

The route boundary is shipped in `@ssgoi/solid/solidstart`; no local component is needed.

Verify: navigate `/` → any page slides in from the right; back slides out.

## Beyond the basics

For a persistent `/products` shell, collapse that family at the root:

```tsx
const getRootTransitionId = (path: string) =>
  path === "/products" || path.startsWith("/products/")
    ? "/products"
    : path;

<SsgoiRouteBoundary resolve={({ pathname }) => ({ id: pathname, key: getRootTransitionId(pathname) })}>
  {props.children}
</SsgoiRouteBoundary>
```

Then add a default pathname boundary only around the changing child:

```tsx
// src/routes/products.tsx
<ProductHeaderAndTabs />
<SsgoiRouteBoundary>
  {props.children}
</SsgoiRouteBoundary>
```

The stable outer key preserves the shell; the inner pathname boundary produces
child OUT/IN. Holding only one boundary constant preserves state but produces
no child transition.

- Full example: https://github.com/meursyphus/ssgoi/tree/HEAD/templates/solidstart
- Framework index: https://ssgoi.dev/llms/frameworks.txt

`@ssgoi/solid/solidstart` loads only its optional Solid Router 0.15–0.16 peer.
The root `@ssgoi/solid` import has no router dependency. The boundary accepts
`as`, DOM attributes, `routeKey`, and `resolve({ pathname })`. Return `{ id, key? }`;
key defaults to id and routeKey overrides it. Query/hash-only changes keep the
boundary by default. Keep the logical id current even when the key is stable.

Manual/custom-router guide: https://ssgoi.dev/llms/manual-boundaries.txt
